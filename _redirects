# Cloudflare Pages _redirects file for SPA routing
# Specific route for private notes must come first
/priv/*    /index.html   200

# Handle all other routes (must come after specific routes)
/*         /index.html   200
